# LogMachine Central Deployment

A central server for collecting and storing your logs.

## Quick Install

```bash
cd ~ # Or any directory you want to install the central server in
git clone https://github.com/logmachine/opencentral.git central
cd central && deployment/install.sh [options] # See documentation at [logmachine](https://logmachine.org/docs/setup)
```
